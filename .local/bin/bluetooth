#!/bin/sh

[ -z "$(bluetoothctl info | grep Connected:\ yes)" ] && bluetoothctl connect 00:00:00:01:CC:C7 || bluetoothctl disconnect 00:00:00:01:CC:C7


pkill -RTMIN+14 "${STATUSBAR:?}" &
pkill -RTMIN+10 "${STATUSBAR:?}" &
